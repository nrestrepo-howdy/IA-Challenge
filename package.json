{
  "name": "verbo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "gate": "node tools/evidence/ac-gate.mjs",
    "verify": "npm run typecheck && npm run test && npm run gate"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "acorn": "^8.14.0",
    "acorn-walk": "^8.3.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
