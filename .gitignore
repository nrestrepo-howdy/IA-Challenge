node_modules/
dist/
# .verbo/events.jsonl is NOT ignored: it is the parallelism and autonomous-loop
# evidence the submission requires, and evidence that lives only on one machine is
# not evidence. Secrets are stripped at write time by .claude/hooks/log-event.sh.
.env
.env.local
*.local

.npm-cache/

# Agent worktrees live inside the repo; they are never part of it.
.claude/worktrees/

dist/

test-results/
playwright-report/

# Nor are the nightly evaluation results: the useful number is the curve across
# nights, and a curve nobody else can see is a claim.
