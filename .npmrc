# Project-local npm cache. The default (~/.npm) is not always writable, and an
# install that fails on a permissions error is a reproducibility bug, not an
# environment quirk: a judge should not have to debug it.
cache=./.npm-cache
fund=false
audit=false
